services:
  postgres:
    image: 'postgres:latest'
    ports:
      - '127.0.0.1:5432:5432'
    restart: always
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
